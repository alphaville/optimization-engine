# -----------------------------------------------------------------
#
# Autogenerated Cargo.toml configuration file for TCP interface
# This file was generated by OptimizationEngine
#
# TCP interface for {{meta.optimizer_name}} v{{meta.version}}
#
# See https://alphaville.github.io/optimization-engine/
#
# -----------------------------------------------------------------

[package]
name = "tcp_iface_{{meta.optimizer_name}}"
version = "0.0.1"
license = "MIT"
authors = ["John Smith"]
edition = "2018"
publish=false


[dependencies]
clap = "2"
{% if build_config.local_path is not none -%}
optimization_engine = {path = "{{build_config.local_path}}"}
{% else -%}
optimization_engine = "{{build_config.open_version or '*'}}"
{% endif %}

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pretty_env_logger = "0.3.0"
log = "0.4.6"
{{meta.optimizer_name}} = { path = "../" }

